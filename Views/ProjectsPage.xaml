<Page x:Class="Zerve.Views.ProjectsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with Add Button -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Projects" 
                      FontSize="28" 
                      FontWeight="SemiBold"
                      Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"
                      VerticalAlignment="Center"/>
            <ui:Button x:Name="AddProjectButton"
                      Content="Add Project" 
                      Icon="{ui:SymbolIcon Add24}"
                      Appearance="Primary"
                      HorizontalAlignment="Right"
                      Padding="16,10"/>
        </Grid>

        <!-- Projects List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ProjectsStackPanel"/>
        </ScrollViewer>

        <!-- Empty State -->
        <StackPanel Grid.Row="1" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   x:Name="EmptyState">
            <ui:SymbolIcon Symbol="FolderOpen24" FontSize="64" Margin="0,0,0,16" Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"/>
            <TextBlock Text="No projects yet" 
                      FontSize="20" 
                      FontWeight="SemiBold"
                      Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                      Margin="0,0,0,8"/>
            <TextBlock Text="Click 'Add Project' to get started" 
                      FontSize="14"
                      Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"/>
        </StackPanel>
    </Grid>
</Page>
